import styled from "styled-components";

export const HomeSection = {
  HomeFrame:styled.div`
    position: relative; width:100%;

    #map {min-height:91vh;
      &.sub {min-height:20vh;}
    }
  `
}